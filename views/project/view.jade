extends ../mainLayout
//Project Home
block content
  .container
    .col-md-3
      h3 #{page.project.name}
      hr
      ul.nav.nav-pills.nav-stacked
        li.active: a(href='/project/' + page.project.id + '-' + page.project.name) About
        li: a(href='/project/' + page.project.id + '-' + page.project.name + '/members') Member Groups
        li: a(href='/project/' + page.project.id + '-' + page.project.name + '/tasks') To Do
        li: a(href='/project/' + page.project.id + '-' + page.project.name + '/events') Events
    .col-md-9
      block projectContent
        h1 About #{page.project.name}
        hr
        h5 #{page.project.description}
